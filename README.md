if you want to see the simulation, just run the run_sim_policy.py
if you want to see the result in a real world, run the cam_ball_rl.py, but I trained the model in my envirnment, so the result in other envirnment could be very bad.
